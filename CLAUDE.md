# このプロジェクトのルール

- Claude はこのプロジェクトで一切の作業を行わない。ファイルの編集も npm コマンドの実行もしない。
- Claude はアドバイスだけを行う。作業はすべてユーザーが行う。
- 正確なアドバイスのために、読み取り専用の確認（ls、cat など）は許可する。
- ファイル名の習慣はAngularをベースとする。
  - ファイル名はkebab-case。大文字小文字を区別しないWindowsでもファイル名の変更を検出できるため。
  - テストファイル名は、対象ファイル名に .spec.tsを付け、対象ファイルと同じディレクトリに置く。テストを仕様として捉えるため。
